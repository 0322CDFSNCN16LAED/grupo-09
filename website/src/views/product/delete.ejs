<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../partials/head") %>
    </head>

    <body class="formularioAdminBody">
        <main>
            <div class="container-xxl login-register admin">
                <div class="rowAdminForm">
                    <div class="col login-logo admin">
                        <img src="/images/logo-nobg.png" />
                    </div>
                </div>
                <div class="row adminRow">
                    <section
                        class="formulario form-login adminForm"
                        style="padding: 1%">
                        <div class="aboutToDelete">
                            <h4>
                                El producto que estás intentando eliminar es <%=
                                tourBuscado.name %>, coloca su ID (<%=
                                tourBuscado.id%>) aqui abajo.
                            </h4>
                        </div>

                        <form
                            action="/product/delete/<%= tourBuscado.id %>?_method=delete"
                            method="post">
                            <label>ID del tour a eliminar:</label>
                            <input
                                class="input-login"
                                type="number"
                                name="id"
                                value="<%= tourBuscado.id %> " />
                            <input
                                class="btn-login"
                                type="submit"
                                value="Eliminar" />
                        </form>
                    </section>
                </div>
            </div>
        </main>
    </body>

    <!--
    <body>
        <h1>
            El producto que estás intentando eliminar es <%= tourBuscado.name
            %>, ID numero <%= tourBuscado.id%>
        </h1>
        <form
            action="/product/delete/<%= tourBuscado.id %>?_method=delete"
            method="post">
            <input type="text" name="id" value="<%= tourBuscado.id %> " />
            <input type="submit" value="eliminar" />
        </form>
    </body>-->
</html>
